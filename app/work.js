onmessage = function (image,model) {
  //从1加到num
  // var num = event.data;
  // var result = 0;
  // for (var i = 1; i <= num; i++) {
  //     result += i;
  // }
  console.log(image,model);
  postMessage(result);
}